pipeline{
    agent{ label 'gradle_spc' }
    stages{
        stage('vcs'){
            steps{
                git url: 'https://github.com/archanaraj-m/dummyspring.git',
                    branch: 'main'
            }
        }
        stage('package'){
            tools{
                jdk 'JDK_17_UBUNTU'
            }
            steps{
                sh 'mvn package'
            }
        }
        stage('Build'){
	        steps{
		      sh 'gradle build'
	        }
        }
        
    }
}